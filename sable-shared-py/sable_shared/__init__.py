"""Shared Python library for Sable FastAPI services.

Modules:
    middleware/     ASGI middleware (service auth, request id, logger, error handler)
    config/         env-var loading
    utils/          context propagation, error normalisation, retry helpers
"""
