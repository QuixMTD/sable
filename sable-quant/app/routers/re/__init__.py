"""Property (sable-re) quant surface — mounts under /re.

Module-specific quant only: no daily price series — cap rate, gross /
net rental yield, DSCR, LTV, void-adjusted cash flow, regional growth
scenarios, AVM. Monte Carlo / scenario maths is reused from the shared
primitives, fed property-specific growth distributions.

Empty until sable-re is built (build step 8).
"""
