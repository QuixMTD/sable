# sable-quant runtime deps.
#
# sable-shared (the Python lib) is installed separately from the copied
# source tree in the Dockerfile; for local dev: pip install -e ../sable-shared-py
# Build the image from the REPO ROOT:
#   docker build -f sable-quant/Dockerfile -t sable-quant .

fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3

numpy==2.2.0
pandas==2.2.3
scipy==1.14.1

# GARCH(1,1) conditional volatility (/risk/volatility).
arch==7.2.0
# Factor-model OLS (/factors/regress). Explicit (not just arch's
# transitive pull) since we now depend on it directly.
statsmodels==0.14.6

# Heavier engines (cvxpy general-convex optimisation, numba-accelerated
# paths) land with later slices — not pulled until they're actually used.
